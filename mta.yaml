_schema-version: "3.2"
ID: bpwizard-roofing
version: 0.0.1

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

modules:
  # ---------------------
  # 1. UI Module
  # ---------------------
  - name: bpwizard-roofing
    type: html5
    path: .
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build
      supported-platforms: []

  # ---------------------
  # 2. App Content Module
  # ---------------------
  - name: bpwizard-roofing-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: bpwizard-roofing-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - name: bpwizard-roofing
          artifacts:
            - bpwizard-roofing.zip
          target-path: resources/

  # ---------------------
  # 3. Destination Content Module
  # ---------------------
  - name: bpwizard-roofing-destination-content
    type: com.sap.application.content
    path: .
    requires:
      - name: bpwizard-roofing-dest-srv
        parameters:
          content-target: true
      - name: bpwizard-roofing-repo-host
        parameters:
          service-key:
            name: bpwizard-roofing-repo-host-key
      - name: bpwizard-roofing-uaa
        parameters:
          service-key:
            name: bpwizard-roofing-uaa-key
    parameters:
      content:
        instance:
          destinations:
            - Name: bpwizard-roofing_html_repo_host
              ServiceInstanceName: bpwizard-roofing-html5-service
              ServiceKeyName: bpwizard-roofing-repo-host-key
              sap.cloud.service: bpwizard-roofing
            - Authentication: OAuth2UserTokenExchange
              Name: bpwizard-roofing_uaa
              ServiceInstanceName: bpwizard-roofing-xsuaa-service
              ServiceKeyName: bpwizard-roofing-uaa-key
              sap.cloud.service: bpwizard-roofing
          existing_destinations_policy: update
    build-parameters:
      no-source: true

resources:
  - name: bpwizard-roofing-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: bpwizard-roofing-html5-service

  - name: bpwizard-roofing-uaa
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
      service-name: bpwizard-roofing-xsuaa-service

  - name: bpwizard-roofing-dest-srv
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      service-name: bpwizard-roofing-destination-service
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update